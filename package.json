{
  "name": "poc-premium",
  "version": "0.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify main.js -d -o bundle.js",
    "start": "watchify main.js -d -o bundle.js",
    "install": "npm install browserify -g watchify -g bower -g & bower install"
  },
  "author": "",
  "private": true
}
